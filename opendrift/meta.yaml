{% set name = "opendrift" %}
{% set version = "1.4.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  git_url: https://github.com/OpenDrift/opendrift.git
  git_rev: "v{{ version }}"

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"
  noarch: python

requirements:
  build:
    - python>=3.7
    - pip
    - setuptools
    - setuptools_scm

  run:
    - python>=3.7
    - numpy
    - scipy
    - matplotlib
    - netCDF4
    - pyproj
    - cartopy
    - nc-time-axis
    - xarray
    - dask
    - xhistogram
    - opendrift-landmask-data>=0.6
    - oil_library-opendrift>=1.1.1.4
    - gdal>=3.1
    - coloredlogs

about:
  home: https://github.com/OpenDrift/opendrift
  license: GPLv2
  summary: Open source framework for ocean trajectory modelling


